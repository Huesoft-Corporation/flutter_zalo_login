name: flutter_zalo_login_example
description: Demonstrates how to use the flutter_zalo_login plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_zalo_login:
    path: ../
flutter:

  uses-material-design: true

